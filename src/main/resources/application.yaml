server:
  port: 3333
spring:
  application:
    name: RegisterAndLogin
  datasource:
    password: password
    url: jdbc:postgresql://localhost:7374/postgres
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: true
